[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
# Default rules; ignore E501 (line-too-long) — agent prompts are naturally long
select = ["E", "F", "W"]
ignore = ["E501"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
